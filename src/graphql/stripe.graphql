mutation CreatePaymentIntent($data: Plan) {
  createPaymentIntent(data: $data) {
    data {
      clientSecret
      paymentIntentId
      amount
      currency
      status
      livemode
    }
    status
    message
  }
}
